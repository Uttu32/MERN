<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=name%></title>
  </head>
  <body>
    <h1>Hey</h1>
    <h3>
      <%=name%> <%if(isPremium) {%>
      <span>🌟</span>
      <% } %>
    </h3>
    <h2>Hobby</h2>
    <h4>
       <% hobby.forEach((hobby) => { %>
        <ul>
            <li><%= hobby %></li>
        </ul>
       <% }) %>
    </h4>
  </body>
</html>
